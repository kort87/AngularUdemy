import { SortServerPipe } from './sort-server.pipe';

describe('SortServerPipe', () => {
  it('create an instance', () => {
    const pipe = new SortServerPipe();
    expect(pipe).toBeTruthy();
  });
});
